[project]
name = "gg"
version = "0.1.0"
description = "Goals & guidelines — boundary enforcement hooks for Claude Code"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
