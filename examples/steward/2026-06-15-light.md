---
type: steward-read
date: 2026-06-15
depth: light
system: 06-orchestrator (event bus + dispatch + replay)
gap-score: 2/7
intent-refresh-needed:
---

# Part 1 — Own read
*Written before looking at any artifacts.*

**Trigger:** Weekly light read — first one since the crash-recovery sprint ended.

**What I think the current state is:**
The orchestrator dispatches events to consumers over a bus. Consumers can
replay from checkpoints up to 30 days back. We recently had a crash-recovery
bug (V2 failure in the gate run) where a batch re-published duplicates on
restart. That's fixed. The metrics endpoint also grew a `/debug/state` path
during the investigation — I remember it appeared as an orphan in the gate
report but I'm not sure what happened to it.

**What I think changed since last read (2026-06-09):**
- Crash recovery fixed: ack only after bus confirmation
- Consumer-side dedup cache added (the subagent delegation)
- Replay still blocked on escalation 002 (ADR-3 conflict — bus-only consumers)
- Prometheus metrics endpoint in progress, came out of the dashboard triage

**Where I'd worry:**
The replay path is the one I'm least confident in. Escalation 002 was resolved
in favor of the interface rule (consumers stay bus-only; replay goes through
the bus), but I don't know if the implementation caught up. That's the area
where spec drift seems most likely.

**What I'm uncertain about:**
- Whether the `/debug/state` orphan was eventually mapped to a plan item or reverted
- Status of escalation 002's resolution in the actual code
- Whether the dedup cache chose a storage backend (that was a reserved decision)

---

# Part 2 — Compare

**Artifacts read:**
- [x] Drift notes since last read: `specs/06-orchestrator/drift/`
- [x] Gate reports since last read: `specs/06-orchestrator/gates/`
- [x] Guideline changelog: `git log -- CLAUDE.md`
- [x] Open escalations: `specs/06-orchestrator/escalations/`

**Confirmed:**
The gate report (2026-06-15) confirmed the crash-recovery failure (V2 FAIL,
V4 BLOCKED). Escalation 001 is resolved. My read of "replay still blocked"
is confirmed — V4 is blocked on escalation 002. The Prometheus metrics work
came out of the dashboard triage verdict, as I remembered.

**Missed:**
1. The drift note (2026-06-14) recorded a structural drift classification on
   the replay path: `replay.py` touched a spec-named interface during the
   escalation 001 fix. I had no recollection of this — I thought the ack-timing
   fix stayed within the dispatch path.
2. The dedup cache delegation (spec 01-hooks subdelegation) resolved the
   storage backend question as SQLite. I remembered it as "unresolved reserved
   decision." It was answered and the gate passed.

**False positives:**
My read said "metrics endpoint in progress" — it's actually complete and
mapped to a plan item (plan-revision proposed at gate time, per the gate report
orphan note). Not in-progress; landed.

**Perception-gap summary:**
My model of the replay path is behind: a structural drift event happened that
I didn't register, meaning the spec-named interface was touched without my
knowing. That's the meaningful gap. The dedup cache and metrics misses are
minor (I knew they existed; I had the status wrong, not the fact).
