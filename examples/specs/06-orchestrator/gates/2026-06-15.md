---
type: gate-report
spec: 06-orchestrator
date: 2026-06-15
claimed: done
verdict: fail
---
# Gate run

**Claim (recorded before gates ran):** "All requirements complete. Dispatch
latency well under target, crash recovery handled, replay implemented and
tested locally."

## Verification items

| # | Item (from spec) | Check | Proof | Result |
|---|---|---|---|---|
| V1 | p95 dispatch latency < 500ms at 5k events/min | `make bench` | bench/2026-06-15.txt | PASS |
| V2 | consumer crash mid-batch: zero loss, zero duplicates after restart | `pytest tests/test_crash_recovery.py` | excerpt below | FAIL |
| V3 | no message loss at sustained 5k events/min | `make loadtest` | loadtest/2026-06-15.txt | PASS |
| V4 | replay from checkpoint reproduces the bus stream exactly | `pytest tests/test_replay.py` | — | BLOCKED (escalation 002) |
| V5 | config validates on startup; bad config exits non-zero | `pytest tests/test_config.py` | test output | PASS |

V2 failure: `test_crash_mid_batch` — 3 duplicate deliveries after restart.
Recovery re-publishes the full batch instead of resuming from the last ack.

## Claimed vs verified

Claimed done; one of five failed and one is blocked. The gap: crash recovery
was "handled" in code but the duplicate-delivery case was never run — the
claim asserted what the gate then disproved.

## Traceability

| Change | Plan item | Requirement |
|---|---|---|
| src/orchestrator/dispatch.py | plan-3 | R1, R2 |
| src/orchestrator/queue.py | plan-4 | R2 |
| src/orchestrator/replay.py | plan-5 | R5 |
| src/orchestrator/metrics.py | — | — ⚠ orphan |

Orphan: `metrics.py` grew a `/debug/state` endpoint during the 001
investigation; no plan item covers it. Route: plan revision proposed (keep as
diagnostic, map to R6-observability) — or revert before PR.

## Route

- V2 → plan item 4 reopened: resume from last ack on restart.
- V4 → waiting on escalation 002 (replay path vs ADR-3); no PR until resolved.
- Orphan → plan revision proposed, awaiting approval.
